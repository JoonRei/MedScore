"use client";import{useState}from'react';
export function ChangePinForm(){const[busy,setBusy]=useState(false),[msg,setMsg]=useState(''),[ok,setOk]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget),res=await fetch('/api/student/pin',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({currentPin:f.get('currentPin'),newPin:f.get('newPin')})}),b=await res.json().catch(()=>({}));setBusy(false);setOk(res.ok);setMsg(res.ok?'PIN changed successfully.':b.error||'Unable to change PIN.');if(res.ok)e.currentTarget.reset()}return <form className="form-grid" onSubmit={submit}>{msg&&<div className={`alert ${ok?'alert-success':'alert-error'}`} style={{gridColumn:'1 / -1'}}>{msg}</div>}<div className="field"><label>Current PIN</label><input className="input" name="currentPin" type="password" inputMode="numeric" pattern="[0-9]{4,6}" required/></div><div className="field"><label>New PIN</label><input className="input" name="newPin" type="password" inputMode="numeric" pattern="[0-9]{4,6}" required/></div><div className="form-actions"><button className="button button-primary" disabled={busy}>{busy?'Saving…':'Change PIN'}</button></div></form>}
