# MedScores V4.21

- Centers shared #4/#5 leaderboard entries without the empty leading row.
- Replaces the Student score-update popover with a dedicated Notifications page.
- Keeps live in-app result notifications and unread badges.
- Adds opt-in device Web Push for newly released results.
- Uses `public/logo.png` for notification artwork.
- Keeps transient toasts at 3 seconds.

See `SETUP.md` for the one-time notification migration and VAPID key setup.
