import{PageHeader}from'@/components/PageHeader';import{AssessmentsClient}from'@/components/AssessmentsClient';import{createAdminClient}from'@/lib/supabase/admin';import{requireAdmin}from'@/lib/admin-auth';
export default async function Page(){await requireAdmin();const db=createAdminClient();const[{data:a},{data:s}]=await Promise.all([db.from('assessments').select('*,subjects(name,code),scores(id)').order('assessment_date',{ascending:false}),db.from('subjects').select('id,name').eq('is_archived',false).order('name')]);return <><PageHeader eyebrow="Scores" title="Assessments" description="Create quizzes, long exams, pre-tests, post-tests, term exams and other assessment types."/><AssessmentsClient assessments={a||[]} subjects={s||[]}/></>}
